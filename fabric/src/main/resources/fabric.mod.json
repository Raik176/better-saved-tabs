{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "icon": "assets/${id}/icon.png",
  "authors": [
    "${author}"
  ],
  "contact": {
    "issues": "https://github.com/Raik176/better-saved-tabs/issues",
    "sources": "https://github.com/Raik176/better-saved-tabs"
  },
  "license": "${license}",
  "environment": "client",
  "entrypoints": {
    "main": [
      "${group}.${id}.fabric.BetterSavedTabsFabric"
    ]
  },
  "mixins": [
    "${id}-common.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15",
    "minecraft": "${minecraft}"
  }
}